// Tailwind CSS v4 is CSS-first: the whole design system lives in app/globals.css
// via @theme. There is intentionally no tailwind.config.js.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
