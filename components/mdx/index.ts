import { Callout } from './Callout';
import { Figure } from './Figure';

// Components available to every MDX post. Base HTML elements are styled by the
// Tailwind Typography `prose` wrapper on the post page.
export const mdxComponents = {
  Callout,
  Figure,
};
